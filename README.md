# raft-consensus-simulator
A from-scratch, in-process discrete-event simulator for the Raft consensus algorithm (leader election + log replication) with injectable network partitions/delays/crashes, safety-property checkers, and randomized multi-seed property tests.
